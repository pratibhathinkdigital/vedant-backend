<%- include('../../layout/header') %>

    <div class="dashboard-header-banner">
        <div class="banner-welcome-text">
            <h1>Edit Client</h1>
            <p>Update client logo.</p>
        </div>

        <div class="banner-actions">
            <a href="/clients" class="btn btn-secondary">
                <i class="fa-solid fa-arrow-left"></i>
                Back to Clients
            </a>
        </div>
    </div>

    <div class="table-container" style="max-width:900px; margin:20px 0;">

        <div class="section-header">
            <h3>Edit Client</h3>
        </div>

        <form action="/clients/update/<%= client.id %>" method="POST" enctype="multipart/form-data" class="form-wrapper">

            <!-- Current Image -->
            <div class="form-group">

                <label>Current Client Image</label>

                <% if(client.image){ %>
                    <div style="margin-bottom:15px;">
                        <img id="preview" src="<%= client.image %>" style="
                            width:200px;
                            border-radius:10px;
                            border:1px solid #ddd;
                            object-fit:contain;
                        ">
                    </div>
                <% } else { %>
                    <img id="preview" src="" style="
                        display:none;
                        width:200px;
                        border-radius:10px;
                        border:1px solid #ddd;
                    ">
                <% } %>

            </div>

            <!-- Change Image -->
            <div class="form-group">
                <label>Change Client Image</label>
                <input type="file" name="image" class="form-control" accept="image/*">
                <small>Leave empty to keep the current image.</small>
            </div>

            <!-- Buttons -->
            <div class="form-actions">
                <a href="/clients" class="btn btn-secondary">Cancel</a>
                <button type="submit" class="btn btn-primary">Update Client</button>
            </div>

        </form>

    </div>

    <script>

        const imageInput = document.querySelector('input[name="image"]');
        const preview = document.getElementById("preview");

        imageInput.addEventListener("change", function () {
            if (this.files && this.files[0]) {
                preview.src = URL.createObjectURL(this.files[0]);
                preview.style.display = "block";
            }
        });

    </script>

<%- include('../../layout/footer') %>
