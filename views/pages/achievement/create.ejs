<%- include('../../layout/header') %>

    <div class="dashboard-header-banner">
        <div class="banner-welcome-text">
            <h1>Create Achievement</h1>
            <p>Add a new achievement with image and description.</p>
        </div>

        <div class="banner-actions">
            <a href="/achievements" class="btn btn-secondary">
                <i class="fa-solid fa-arrow-left"></i>
                Back to Achievements
            </a>
        </div>
    </div>

    <div class="table-container" style="max-width:900px; margin:20px 0;">
        <div class="section-header">
            <h3>Achievement Details</h3>
        </div>

        <form action="/achievements/store" method="POST" enctype="multipart/form-data" class="form-wrapper">

            <!-- Image -->
            <div class="form-group">
                <label>Achievement Image</label>
                <input type="file" name="image" class="form-control" accept="image/*" required>
                <small>Supported: JPG, JPEG, PNG, WEBP</small>
            </div>

            <!-- Preview -->
            <div class="form-group">
                <img id="preview" src="" style="
                    display:none;
                    width:250px;
                    border-radius:10px;
                    margin-top:10px;
                    border:1px solid #ddd;
                ">
            </div>

            <!-- Description -->
            <div class="form-group">
                <label>Description</label>
                <textarea
                    name="description"
                    class="form-control"
                    rows="4"
                    placeholder="Enter achievement description..."
                    required></textarea>
            </div>

            <!-- Buttons -->
            <div class="form-actions">
                <a href="/achievements" class="btn btn-secondary">Cancel</a>
                <button type="submit" class="btn btn-primary">Save Achievement</button>
            </div>

        </form>
    </div>

    <script>

        const imageInput = document.querySelector('input[name="image"]');
        const preview = document.getElementById("preview");

        imageInput.addEventListener("change", function () {
            const file = this.files[0];
            if (file) {
                preview.src = URL.createObjectURL(file);
                preview.style.display = "block";
            }
        });

    </script>

<%- include('../../layout/footer') %>
